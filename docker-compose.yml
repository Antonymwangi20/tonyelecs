version: '3.8'

services:
  app:
    build: .
    ports:
      - "3001:3001"
    environment:
      NODE_ENV: production
      PORT: 3001
      # Pass your env vars here or use an env_file
      # VITE_SUPABASE_URL: ${VITE_SUPABASE_URL}
      # VITE_SUPABASE_ANON_KEY: ${VITE_SUPABASE_ANON_KEY}
    restart: unless-stopped
